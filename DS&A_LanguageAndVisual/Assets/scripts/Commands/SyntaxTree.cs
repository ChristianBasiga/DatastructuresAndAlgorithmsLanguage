using System.Collections;
using System.Collections.Generic;

namespace DataStructureLanguage.Syntax.SyntaxTree
{
    public class SyntaxTree
    {
        DataStructureLanguage.Syntax.SyntaxNode root;
        

        public bool traverseRight()
        {
            //ToDo: Go to right child of root, if returns true, overwrite current root, then traverse left, otherwise just traverse left.

        }

        public void traverseLeft()
        {
            //ToDo: These are nonblock nodes
        }
    }
}